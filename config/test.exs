use Mix.Config

config :dwolla,
  root_uri: "https://api-sandbox.dwolla.com/",
  oauth_uri: "https://www.sandbox.dwolla.com/oauth/v2/token",
  client_id: "test_id",
  client_secret: "test_secret",
  access_token: nil
